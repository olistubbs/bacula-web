Website written in perl to assist with adding backups to bacula.

Stores all config in the backend, configs will be updated hourly assuming a ".restart" flag exists in the bacula config directory (/etc/bacula)
